# CLAUDE.md

This file intentionally contains no rules of its own.

**`AGENTS.md` in the repository root is the single source of truth** for the tech stack,
project structure, build commands, testing, CI, platform gotchas and the documentation
policy (no `docs/` folder: agent notes go in `AGENTS.md`, user-facing docs go in the six
READMEs). Read it first, and update it instead of adding rules here — duplicated rule
files always drift apart.
